[phases.setup]
nixPkgs = ["python310", "gcc", "glib", "libGL"]

[phases.install]
cmds = ["pip install -r python_services/requirements.txt"]

[phases.build]
cmds = ["echo 'Build complete'"]

[start]
cmd = "bash python_services/start.sh"
