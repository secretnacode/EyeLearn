[build]
builder = "NIXPACKS"
buildCommand = "pip install -r python_services/requirements.txt"

[deploy]
startCommand = "bash python_services/start.sh"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
